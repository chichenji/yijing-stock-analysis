# 易经测股功能完善计划

## Goal
在现有“时间起卦为主、现实数据只作验象”的基础上，把项目补成可解释、可复盘、可批量调用、可供 Agent 稳定集成的商业可用版本。

## 当前判断
- 现有测试通过：`15 passed`。
- 已具备：本卦、动爻、变卦、互卦、错卦、综卦、体用五行、六十四卦全谱统计、Markdown/JSON/HTML 输出、行情/新闻/宏观验象入口。
- 主要缺口：经典卦辞/爻位解释尚未接入报告；评分只有分数没有逐项原因；数据源失败原因不够透明；复盘、批量、画像、JSON Schema 和示例数据还没有产品化。
- 边界保持：默认不使用代码卦或行情卦；股票代码和行情数据只作可选旁参或验象，不包装成传统古法。

## 可加入的功能
- 传统易学增强：周易卦序、上下经、卦辞摘义、象辞摘义、动爻爻位金融转译。
- 可解释评分：每一项加分、扣分、权重、来源、理由都输出到 JSON/Markdown/HTML。
- 数据源状态：行情、新闻、宏观每个源展示 `success/missing/error`、字段覆盖率、失败原因、采集时间。
- 预测记录：每次分析写入 JSONL，保留输入、卦象、评分、结论、数据源状态。
- 复盘统计：按 1d/3d/5d 对真实收益、最大回撤、命中率、卦象偏差做统计。
- 六十四卦历史画像：统计个股常见本卦、变卦、动爻、体用关系、偏多/偏空/警示命中率。
- 批量分析：支持股票列表、CSV、组合权重，生成横向比较报告。
- Agent 集成：输入 JSON Schema、输出 JSON Schema、调用提示词模板、示例数据。
- HTML 交互：折叠经典文本、评分明细、来源追踪、复盘图表，并保持静态 HTML 可直接打开。

## Tasks
- [x] 1. 收口当前未提交改动：保留 `report_html.py` 薄封装、拆分 HTML 模块，接入 `classic_text.py`。Verify: `pytest -q` 通过，默认报告出现“卦辞摘义”和“动爻爻位”，且不出现默认“代码卦/行情卦”。
- [x] 2. 补全传统六十四卦资料：给每卦增加周易序号、上下经、卦辞摘义、象辞摘义、股市象意、风险触发条件。Verify: JSON 中每个核心卦都有 `classic` 字段，六十四卦参考区可展示完整资料。
- [x] 3. 增强动爻解释：按初、二、三、四、五、上分别输出“位置含义、股票白话、确认信号、风险信号”。Verify: 京东方 A HTML 报告中，本卦动爻有独立爻位解释。
- [x] 4. 建立评分拆解模型：新增 `ScoreBreakdown`，记录易经、技术、资金、新闻、宏观每项权重和加减分理由。Verify: JSON、Markdown、HTML 都能看到逐项原因，总分与拆解项可对齐。
- [x] 5. 工程化数据源状态：把行情、新闻、宏观采集改为带状态的结果对象，保留字段覆盖率、provider、错误原因和采集时间。Verify: 网络不可用或 akshare 缺失时，报告明确显示失败原因，不伪造成功。
- [x] 6. 加入预测记录与复盘 CLI：支持 `--record records/predictions.jsonl`，并支持读取价格序列或历史数据生成复盘摘要。Verify: 连续两次分析会追加两行记录，`scripts/backtest.py` 能输出 1d/3d/5d 命中率。
- [x] 7. 加入卦象历史统计和个股画像：从记录中聚合本卦、变卦、动爻、体用、生克、偏性和阶段命中情况。Verify: `yijing-stock profile 000725.SZ --records ...` 输出个股画像 JSON/Markdown。
- [x] 8. 加入批量与组合分析：支持 `batch` 子命令读取 txt/CSV，输出多股票评分、方向、风险、主卦和数据质量对比。Verify: 三只股票样例能生成一个静态 HTML 对比页。
- [x] 9. 补齐 Agent 与示例文件：新增 `schemas/analysis_input.schema.json`、`schemas/analysis_result.schema.json`、`prompts/agent_usage.md`、`examples/market.json`、`examples/news.json`、`examples/macro.json`。Verify: README 中所有示例命令可直接运行。
- [x] 10. 最终验证与发布：运行单元测试、代码体量检查、京东方 A HTML 生成、README 检查、提交并推送。Verify: `pytest -q` 通过，函数不超过 50 行，文件不超过 300 行，GitHub 仓库更新。

## Done When
- [x] 京东方 A 报告能展示总断、五卦、经典摘义、动爻解释、评分拆解、来源状态、六十四卦统计、算法折叠区。
- [x] JSON 输出能被 Agent 直接消费，并包含可复盘字段。
- [x] 数据缺失时报告说清楚缺什么、为什么缺、会影响哪一部分判断。
- [x] 批量、复盘、个股画像至少各有一个可运行样例。
- [x] 文档不包含 MCP 或 Stitch 内容。

## Notes
- 可借鉴 UZI-Skill 的多数据源、报告导出、批量对比和数据质量提示思路，但本项目核心仍是易经象意研判，不做 51 评委投票或自动交易。
- 完整六爻纳甲、机器学习调参、自动交易暂不进入本轮；这些会明显扩大解释边界和测试成本。
